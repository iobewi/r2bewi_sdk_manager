# r2bewi — NTP server pour le LAN robot
# __IP__ et __LAN__ sont substitués par r2bewi init
# Editer : pool NTP upstream si pas d'accès internet

pool pool.ntp.org iburst

# Servir le LAN robot
allow __LAN__
local stratum 10

driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
